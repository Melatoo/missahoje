- event: initialized workspace for Frontend Architecture, altitude=project, purpose=Guide the frontend implementation based on the approved UX
- decision: Deliverable is restricted to a developer-facing Architecture Spine.
  - prevents: Bloat and unnecessary documentation for audiences that don't exist.
- decision: Adopt Feature-based (Vertical Slice) architecture for components over Atomic Design.
  - binds: Frontend repository structure
  - prevents: Deep folder traversal for single features, grouping by type rather than domain.
- decision: Hybrid State Management (Zustand + URL Search Params)
  - binds: State ownership across the frontend
  - prevents: Missing shareable links (URL) or laggy map interactions (React Context).
- decision: Client-Side Fetching with TanStack Query (React Query) + Axios
  - binds: Map bounding box interactions and dynamic data loading.
  - prevents: Over-fetching and heavy server actions for high-frequency map panning.
- event: spine finalized
