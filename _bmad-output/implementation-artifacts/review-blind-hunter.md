Viewed SKILL.md:1-38

Here is the adversarial review of the provided diff. 

- **Missing Assets:** `favicon.ico` was deleted without replacement, and `globals.css` references a `/lourdes.png` background image that is nowhere to be found in the diff. This guarantees 404 network errors and a broken visual aesthetic upon deployment.
- **Runtime Crash Risk:** `NextMassCard.tsx` blindly accesses `nextMass.paroquia.nome` and `nextMass.paroquia.endereco` without optional chaining or null checks. The previous implementation safely handled missing data. This careless assumption will crash the application if the payload shape varies or data is absent.
- **Dead Code Injection:** `LayoutShell.tsx` was created but is entirely orphaned. It is never imported or utilized anywhere in the application, adding pointless clutter to the repository.
- **UX Regression on Schedules:** The `MassSchedule.tsx` rewrite removed the "Fim de Semana" tab which fetched Saturday and Sunday simultaneously. Users are now forced to clumsily toggle between individual days to plan their weekend.
- **Silent State Degradation:** The cookie expiration in `actions.ts` was quietly slashed from 1 year to 30 days without justification. This degrades the user experience by forcing frequent, unnecessary location re-selections.
- **Bloated Dependencies:** `zustand` was added to `package.json` but is completely unused throughout the codebase. Adding dependencies without implementing them is textbook sloppy maintenance.
- **Abysmal CSS Hacks:** Faking a dark overlay by applying a 2000px inset `box-shadow` directly to the `body` element in `globals.css` is an unoptimized, hacky approach that can trigger layout thrashing and rendering penalties on lower-end devices.
- **Missing Server Revalidation:** `setLocationCookie` in `actions.ts` dropped its `revalidatePath('/')` call. Relying exclusively on client-side `router.refresh()` risks serving stale SSR HTML to users hitting the application directly or navigating from external links.
- **Unsafe Data Fetching:** `CitySelector.tsx` removed the `active` boolean flag from its `useEffect` cleanup. Rapidly toggling the selector before the network request resolves will now trigger state updates on unmounted or stale component states, leading to memory leaks and React warnings.
- **Broken Search Queries:** If `nextMass.paroquia.endereco` in `NextMassCard.tsx` is null or undefined, the Google Maps link concatenation will literally query the string `"undefined"`, resulting in a broken mapping experience for the user.
- **Wasteful Font Loading:** `layout.tsx` imports the `Cinzel` font and maps it to a `--font-title` variable. Since the only component that uses this variable (`LayoutShell.tsx`) is dead code, the font is downloaded and parsed by the client for absolutely no reason, needlessly punishing load times.